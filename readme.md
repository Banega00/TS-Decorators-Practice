# Decorators

Decorators are just a functions

! set **experimentalDecorators** to **true** in tsconfig.ts

Types of decorator:
- Class Decorator
- Property Decorator
- Method Decorator
- Parameter Decorator